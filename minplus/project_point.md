Project a point onto tropical surface. This library assumes that the first coordinate of the point is defining the offset from the surface.

Parameters
: `point : tuple`: the point to be projected.

Returns
: `result : tuple`: the projected point.
