# hyperplane_from_apex(point : Collection)
Returns a tropical hyperplane based on its apex.

## Parameters
- `point : Collection`: the apex of the hyperplane.

## Returns
- `result : Hyperplane`: the tropical hyperplane.
